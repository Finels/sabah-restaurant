<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ include file="common/taglib.jsp" %>
<%--右侧顶部导航菜单--%>
<%--<img src="${ctx}/images/system_ico4.png"/>
<c:forEach var="topMenu" items="${topMenus}">
    <c:if test="${topMenu.uniqueCode == _topMenu}">
        ${topMenu.name}
    </c:if>
</c:forEach>
>
<c:forEach var="subItem" items="${systemMenus}">
    <c:if test="${subItem.uniqueCode == systemMenu}">
        ${subItem.name}
    </c:if>
</c:forEach>

<c:forEach var="thirdItem" items="${thirdMenus[_secondMenu]}">
    <c:if test="${thirdItem.uniqueCode == _thirdMenu}">
        > ${thirdItem.name}
    </c:if>
</c:forEach>--%>
